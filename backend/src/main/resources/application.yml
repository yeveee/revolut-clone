spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/revolut
    username: revolut
    password: ${DB_PASSWORD:revolut}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: ${JWT_SECRET:kfLLoUIwcdMRP7tPzrsMWFoEyYWXc3RV5CNJ8PWledg=}
  expiration-ms: 3600000

server:
  port: 8080
